union-find
   1，动态连通性：
             （p,q）一对整数对，每一整数表示一个某种类型的对象.
                    p、q使相连的，相连是一种对等的关系      
                    当且仅当两个对象相连时才属于同一个等价类
               对象--触点
               整数对--连接
               等价类--连通分量
               
   2，QuickFind:
           (1)p and q are connected if and only they have the same id.
           (2)find:用find方法查询得给定p的所属组号；
              connect:判断p/q的组号是不是相同
              union:如果组号相同，可以合并；
              如果不相同，遍历id[]，用p的分量重命名q的名称；
   3，QuickUnion:(QuickFind的替换算法，更快):
           (1)由p,q的链接分别找到它们的根触点，
              再将一个根触点链接到另一个即可将一个分量重命名为另一个分量。
              图见142页，图1.5.4（算法第4版）
          （2）find：check if p and q have the same root 
               union:设置p的根触点的id为q的根触点的id
           (连根触点)
          （3）一棵树的大小是它的节点的数量
               树中一个节点的深度是它到根节点的路径上链接数
               树的高度是它的所有节点中的最大深度
    4,WeightedQuickUnion:
           (1)避免获得高的树；
              注意每棵树的对象的数量；
              将小树放到大树下面（***）