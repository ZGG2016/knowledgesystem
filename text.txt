

from pyspark.accumulators import AccumulatorParam

hadoop fs -cat /.../part-*.lzo | lzop -d  读hdfs中的数据

hadoop fs -text /.../part-*.lzo >localfile

cat pp_0129287aa4681886293 |jq -c '.examination_record_strategy[]'|jq .|vim -


nohup spark-submit trace.py $1 ${ppin}${partno} '/user/yi/ppsample'
